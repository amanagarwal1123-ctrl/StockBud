<analysis>**original_problem_statement:** The user is experiencing a critical issue where uploading a large Excel file (, 24MB) works perfectly in the preview environment but consistently fails in the deployed production environment. The user suspects it might be related to recent code modularization. The goal is to diagnose and fix this discrepancy.

Additionally, the user provided feedback and requested new features during the debugging process:
1.  Historical uploads should not affect the main dashboard's transaction counts or profit calculations.
2.  A new feature for historical profit analysis is needed in the Visualisation tab, with views by Customer, Supplier, Item, Month, and Year.
3.  Profit calculations must be accurate, addressing issues like negative tunch, negative labor profit, and grouping of mapped items.
4.  The Item Mapping page should show unmapped items from historical data to allow for better data consolidation.
5.  A smaller purchase file was successfully uploaded, but its data was not appearing in the historical summary view.

**User's preferred language**: English

**what currently exists?**
The application has a feature for uploading current (e.g., daily sales) and historical transaction data. The historical data upload was failing in the deployed environment. A significant amount of work has been done to make the upload process robust by moving from a filesystem-based chunk storage to a MongoDB-based one, reducing chunk sizes, and adding frontend retries.

A new Profit analysis tab has been added to the Data Visualization page, which calculates and displays profit metrics from the  collection. This feature has undergone several iterations to fix complex business logic bugs related to returns, cost basis calculation, and item mapping.

The core issue of large file uploads failing in the deployed environment was finally traced to Out-of-Memory (OOM) errors in the background processing worker.

**Last working item**:
-   **Last item agent was working**: The agent was fixing the large file upload failure in the deployed environment. The root cause was identified as an Out-of-Memory (OOM) error during background processing, where the entire 24MB reassembled file was loaded into memory for Pandas to parse. The fix involved changing the  task to write the file chunks to a temporary file on disk first, and then have Pandas read from that file path, significantly reducing peak memory consumption. A stale task cleanup mechanism was also added on server startup.
-   **Status**: IN PROGRESS (The fix has been implemented but has not been verified by the user in the deployed environment yet).
-   **Agent Testing Done**: Y (The new disk-based processing flow was tested and verified in the preview environment).
-   **Which testing method agent to use?**: Manual verification by the user on the deployed app is the next step. If it fails, the agent should use the diagnostic logs.
-   **User Testing Done**: N

**All Pending/In progress Issue list**:
-   **Issue 1 (P0):** Sales file upload fails in the deployed version.
    -   **Attempted fixes:**
        1.  **Failed:** Switched from filesystem to MongoDB for chunk storage. This fixed multi-pod inconsistency but didn't solve the final processing failure.
        2.  **Failed:** Reduced frontend chunk size to 768KB, then to 200KB with retries. This fixed proxy/upload errors but the background processing still failed.
        3.  **In Progress:** Re-architected background processing ( in ) to stream chunks to a temporary file on disk before parsing, to avoid OOM errors. This is the current implemented fix awaiting user validation.
    -   **Next debug checklist:**
        1.  Ask the user to try uploading the large sales file again in the deployed environment.
        2.  If it fails, check the application logs for errors from the background worker. Specifically look for logs indicating the start and end of , memory usage warnings, or OOM killer messages if available.
        3.  Use the  endpoint to see the state of  if a task gets stuck in PROCESSING.
    -   **Why fix this issue and what will be achieved with the fix?**: This is the original and most critical bug. Fixing it will allow the user to upload large historical datasets, which is essential for the historical profit analysis features.
    -   **Status**: USER VERIFICATION PENDING
    -   **Is recurring issue?**: Y
    -   **Should Test frontend/backend/both after fix?**: Backend (primarily by observing the successful processing of the uploaded file).
    -   **Blocked on other issue**: None

**In progress Task List**:
There are no partially completed tasks. The main focus is on resolving the final upload issue.

**Upcoming and Future Tasks**
-   **Upcoming Tasks:**
    -   **P0: User-guided Item Mapping:** The user needs to use the now-functional Item Mapping page to map the 219 unmapped historical items. The agent should guide the user if they have questions. This will improve the accuracy of the historical profit analysis.

**Completed work in this session**
-   **Robust Chunked Upload System (Backend):**
    -   Replaced local filesystem storage for upload chunks with MongoDB collections (, ) to support a stateless, multi-pod deployment architecture.
    -   Implemented chunk verification in  to prevent processing of incomplete files.
    -   Files: .
-   **Memory-Efficient Background Processing:**
    -   Modified  in  to stream file chunks to a temporary file on disk instead of assembling in memory, preventing OOM crashes.
    -   Added a startup task to clean up stale uploads that were stuck in PROCESSING state due to worker crashes.
    -   Files: .
-   **Robust Chunked Upload System (Frontend):**
    -   Reduced upload chunk size to 200KB.
    -   Implemented a retry mechanism with exponential backoff for failed chunk uploads.
    -   Improved visual feedback by disabling the upload dropzone during an active upload.
    -   Files: , .
-   **Historical Profit Analysis Feature:**
    -   Created a backend endpoint  with aggregation pipelines for detailed profit analysis.
    -   Created a Profit tab in the frontend with multiple views (Yearly, Customer, Supplier, Item, Month) using charts and tables.
    -   Files: , ,  (new).
-   **Critical Business Logic Bug Fixes:**
    -   Corrected profit calculations to handle product returns properly (negative ) and avoid double-negating values.
    -   Fixed cost basis calculation to use only  transactions, preventing distorted per-unit costs from returns.
    -   Refactored the entire profit logic to aggregate data by mapped  instead of raw item names, providing a consolidated view.
    -   Files: .
-   **Item Mapping Enhancement:**
    -   Fixed the  endpoint to include items from , making all 219 unmapped historical items visible on the mapping page.
    -   Files: .
-   **Historical Summary Bug Fix:**
    -   Fixed the  endpoint to correctly handle records with  year values, which previously caused the summary to appear empty.
    -   Files: .

**Earlier issues found/mentioned but not fixed**
None. All issues identified during this session were addressed.

**Known issue recurrence from previous fork**
N/A

**Code Architecture**


**Key Technical Concepts**
-   **Backend**: Python (FastAPI), MongoDB (Motor for async operations), Pandas.
-   **Frontend**: React,  for API calls.
-   **Architecture**:
    -   The application uses a stateless, multi-pod backend architecture in deployment.
    -   **Chunked File Upload**: Large files are split into smaller chunks (200KB) on the client and uploaded individually.
    -   **Shared State**: MongoDB is used as the shared state manager for storing upload sessions and file chunks, which is critical for the multi-pod environment.
    -   **Background Processing**: A background task () is triggered to reassemble and process the file. This process was re-architected to be memory-efficient by using the local disk for temporary file reassembly.

**key DB schema**
-   : { , , , , , Wed Feb 11 12:00:36 UTC 2026 }
-   : { , , , , , Wed Feb 11 12:00:36 UTC 2026,  }
-   : { ,  }
-    (new): { , , , ,  }
-    (new): { , ,  }

**changes in tech stack**
No changes in the core tech stack, but a significant architectural pattern change was introduced: moving from a local filesystem-based temporary storage for uploads to a distributed, MongoDB-based storage system. The final implementation uses a hybrid approach: MongoDB for chunk coordination and temporary disk storage on the processing worker for memory-efficient reassembly.

**All files of reference**
-   : The heart of the backend logic. Updated for MongoDB uploads, memory-efficient processing, historical profit analysis, and numerous bug fixes.
-   : Main UI for historical uploads. Updated with smaller chunk sizes and retry logic.
-   : Reusable component for file uploads. Also updated with smaller chunks and retries.
-   : Container for analytics tabs. Updated to include the new Profit tab.
-   : New file containing all UI and logic for the historical profit analysis feature.

**Areas that need refactoring**:
- The profit calculation logic in  ( and its callers) has become very complex due to multiple bug fixes and logic changes. It could benefit from being broken down into smaller, more testable functions.

**key api endpoints**
-   : Initializes a new chunked upload session in MongoDB.
-   : Uploads a single file chunk to MongoDB.
-   : Triggers the background task to process the uploaded file.
-   : Polls for the status of an upload.
-   : Handles direct (small) historical file uploads.
-   : Provides a summary of stored historical data.
-   : New endpoint that provides aggregated profit data from historical transactions.
-   : New endpoint to help debug stuck uploads.

**Critical Info for New Agent**
-   **The primary remaining task is to confirm that the latest fix for large file uploads works in the DEPLOYED environment.** The issue was OOM errors, and the fix involves using temporary disk storage during processing.
-   The user has been through many failed attempts. Be clear and confident in your explanation of the latest fix and guide them through one final test.
-   The historical profit analysis is a complex feature built on user feedback. Be aware of the logic for handling returns, calculating cost basis, and using item mappings, as these were sources of major bugs.
-   After the upload issue is resolved, the next step is to assist the user in mapping their 219 unmapped historical items using the Item Mapping page.

**documents and test reports created in this job**
-   

**Last 10 User Messages and any pending HUMAN messages**
1.  **User:** Sales file upload is still failing in deployed. (Status: Addressed with final OOM fix)
2.  **User:** The purchase entry now appears, but the sales file upload failed. (Status: Acknowledged, led to final OOM fix)
3.  **User:** The sales records are still not uploading, and purchase file entries aren't being created. Asks why the bug can't be pinpointed. (Status: Addressed, was two separate bugs - summary view and upload failure)
4.  **User:** Historical sales data is not uploading, please upload it yourself carefully. (Status: Agent debugged instead of manually uploading, leading to fixes)
5.  **User:** The unmapped items are not showing up in the item mapping page. (Status: Fixed endpoint to query historical data)
6.  **User:** Prefers Option 1: building a tool to let them map the items themselves. (Status: Implemented by fixing the item mapping page)
7.  **User:** Asks about how to map historical items. (Status: Addressed with options, led to fixing the item mapping page)
8.  **User:** While calculating profits, items should be clubbed based on mappings. (Status: Implemented, profit logic refactored)
9.  **User:** Why is labour profit negative? (Status: Fixed, cost basis logic was flawed)
10. **User:** Why is the 'buy tunch' negative? (Status: Fixed, calculation had a double-negative error)

**Project Health Check:**
-   **Broken**: The large file upload feature in the deployed environment is the primary broken component, though a promising fix is in place awaiting validation.
-   **Mocked**: No mocked components.

**3rd Party Integrations**
-   None explicitly mentioned or added in this session. The application uses a standard Python/React stack with MongoDB.

**Testing status**
-   **Testing agent used after significant changes:** YES (Used after implementing the Profit Analysis feature, passed all tests).
-   **Troubleshoot agent used after agent stuck in loop:** NO.
-   **Test files created:** .
-   **Known regressions:** None. The fixes were incremental and did not appear to break existing functionality.

**Credentials to test flow:**
Standard login credentials should be used.
-   **Username**: 
-   **Password**: 

**What agent forgot to execute**
-   The agent correctly identified and attempted to fix all user-reported issues and requests. The core problem was just exceptionally difficult to debug due to the differences between the preview and deployed environments (multi-pod architecture, memory limits). All requested tasks were eventually addressed.</analysis>
